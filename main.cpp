//
//  main.cpp
//  Term 2 Project
//
//  Created by Tunde Taiwo on 14/01/2019.
//  Copyright © 2019 Tunde Taiwo. All rights reserved.
//

#include <iostream>
#include <random>
using namespace std;

/* bool inCircle(double x, double y)
{
    return ((x-0.5)*(x-0.5) + (y-0.5)*(y-0.5)) <= 0.25;
}

int main()
{
    default_random_engine gen;    // underlying (integer) random engine
    uniform_real_distribution<double> dist(0.0, 1.0);   // the desired distribution (in this case U[1,0])
    
    const int n = 1000000;
    int countInCircle = 0;
    for (int i = 0; i < n; i++)
    {
        double x = dist(gen);
        double y = dist(gen);
        if (inCircle(x, y)) countInCircle++;
    }
    cout << 4.0 * (double)countInCircle / (double)n;
}

*/


